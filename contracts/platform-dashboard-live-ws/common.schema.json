{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://greenhouse.local/contracts/platform-dashboard-live-ws/common.schema.json",
  "title": "Frontend WebSocket — Shared Definitions",
  "description": "Shared $defs reused across the frontend WebSocket frame schemas: the connectivity enum, the activity-feed event kind/severity enums, and the metric→unit-bound sensor reading and actuator sample carried by telemetry frames. Mirrors the conventions of contracts/controller-platform-telemetry-mqtt and contracts/platform-dashboard-rest (RFC-007 units, closed enums); kept local rather than cross-contract $ref'd, matching platform-dashboard-rest.",
  "$defs": {
    "connectivity": {
      "type": "string",
      "enum": ["online", "degraded", "offline"],
      "description": "A greenhouse's controller connectivity as the platform sees it: online = fresh telemetry; degraded = a non-critical fault or stale stream; offline = no contact. Offline is data absence, not a fault. The same enum the platform-dashboard-rest contract carries."
    },
    "event_kind": {
      "type": "string",
      "enum": ["fault", "interlock", "profile_applied", "setpoint_edit", "drift", "optimizer_plan_applied", "optimizer_plan_escalated", "optimizer_resolved", "optimizer_run_failed"],
      "description": "Closed set of activity-feed event kinds surfaced to the operator (frontend data model §4). Matches the platform-dashboard-rest EventEntry kind. The four `optimizer_*` kinds ride the live `event` frame stamped `source: optimizer`: `optimizer_plan_applied` (an applied plan, itself a setpoint write) plus the escalation-lifecycle + run-failure audit events `optimizer_plan_escalated`, `optimizer_resolved`, and `optimizer_run_failed`. The actionable set of open holds stays in the polled optimizer console queue, not the stream."
    },
    "event_severity": {
      "type": "string",
      "enum": ["info", "warning", "critical"],
      "description": "The platform's dashboard grading: info = informational; warning = degraded but operating; critical = raises a toast. Distinct from the controller's fault severity (warning/alarm)."
    },
    "metric": {
      "type": "string",
      "enum": ["temperature", "humidity", "co2", "par", "vpd", "soil_moisture"],
      "description": "Closed set of measured/derived quantities (RFC-007), matching the MQTT sensor-reading contract. vpd is derived from temperature + humidity."
    },
    "unit": {
      "type": "string",
      "enum": ["°C", "%RH", "ppm", "µmol·m⁻²·s⁻¹", "kPa", "VWC"],
      "description": "Unit of measure. Bound to the metric by the conditional clauses in `reading` (RFC-007 units convention)."
    },
    "reading": {
      "type": "object",
      "description": "One metric sample in a telemetry frame: a value with its unit bound to the metric, the same metric→unit binding the MQTT sensor-reading contract enforces. The sample time is the frame envelope `ts`.",
      "properties": {
        "metric": { "$ref": "#/$defs/metric" },
        "value": {
          "type": "number",
          "description": "Numeric reading in the unit fixed for this metric."
        },
        "unit": { "$ref": "#/$defs/unit" }
      },
      "required": ["metric", "value", "unit"],
      "allOf": [
        { "if": { "properties": { "metric": { "const": "temperature" } } }, "then": { "properties": { "unit": { "const": "°C" } } } },
        { "if": { "properties": { "metric": { "const": "humidity" } } }, "then": { "properties": { "unit": { "const": "%RH" } } } },
        { "if": { "properties": { "metric": { "const": "co2" } } }, "then": { "properties": { "unit": { "const": "ppm" } } } },
        { "if": { "properties": { "metric": { "const": "par" } } }, "then": { "properties": { "unit": { "const": "µmol·m⁻²·s⁻¹" } } } },
        { "if": { "properties": { "metric": { "const": "vpd" } } }, "then": { "properties": { "unit": { "const": "kPa" } } } },
        { "if": { "properties": { "metric": { "const": "soil_moisture" } } }, "then": { "properties": { "unit": { "const": "VWC" } } } }
      ],
      "additionalProperties": false
    },
    "actuator_name": {
      "type": "string",
      "enum": [
        "heater",
        "fans",
        "roof_vents",
        "misters",
        "co2_injector",
        "grow_lights",
        "shade_screen",
        "irrigation_valve"
      ],
      "description": "Closed set of actuators (physical-system-single.md §Outputs); the same names the MQTT, platform-controller-control-rest, and platform-dashboard-rest contracts use. Kept local rather than cross-contract $ref'd, matching this contract's convention."
    },
    "actuator_sample": {
      "type": "object",
      "description": "One actuator sample in a telemetry frame: commanded vs observed position (the output-side channel, controller safety §5). Mirrors the platform-dashboard-rest ActuatorState, with the sample time carried by the frame envelope `ts`. Position is a 0–100 number: variable/modulating actuators report their level (% open / speed / dim); pure on/off devices (misters, co2_injector, irrigation_valve) report 0 (off) or 100 (on) — the platform's flattening of the MQTT {on, level_pct} shape.",
      "properties": {
        "actuator": { "$ref": "#/$defs/actuator_name" },
        "commanded": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Commanded position/level for this sample (0–100; on/off devices use 0=off, 100=on)."
        },
        "observed": {
          "type": ["number", "null"],
          "minimum": 0,
          "maximum": 100,
          "description": "Observed (readback) position (0–100), or null when the readback is unavailable."
        }
      },
      "required": ["actuator", "commanded", "observed"],
      "additionalProperties": false
    },
    "time_scale": {
      "type": "number",
      "minimum": 0.25,
      "maximum": 32.0,
      "description": "Simulation-only: the greenhouse controller's current simulated-clock speed multiplier (1.0 = real-time; <1 slow-motion; >1 fast-forward). Each controller's clock is independent. Drives the dashboard's per-greenhouse speed indicator; absent on real-hardware controllers. Mirrors the controller's `system-state.simulation.time_scale` and the platform-controller-control-rest `TimeScale.scale` (controller HAL §7)."
    }
  }
}
