{
  "NotFound": {
    "description": "The greenhouse does not exist.",
    "content": {
      "application/json": {
        "schema": { "$ref": "./schemas/common.json#/Error" }
      }
    }
  },
  "ValidationFailed": {
    "description": "A value is out of range or violates a setpoint invariant (e.g. humidity_low_pct < humidity_high_pct); the body names the violated bound. The submission is not recorded as intended state.",
    "content": {
      "application/json": {
        "schema": { "$ref": "./schemas/common.json#/ValidationError" }
      }
    }
  },
  "ServiceUnavailable": {
    "description": "No intended-state baseline could be established: the greenhouse has no prior intended state (a cold start) and its controller could not be reached to seed one, so the submission was NOT recorded. This is distinct from the offline-but-recorded case, which returns 202 (the change is held and re-asserted on reconnect). Transient — retry on a later cadence once a baseline exists or the controller reconnects.",
    "content": {
      "application/json": {
        "schema": { "$ref": "./schemas/common.json#/Error" }
      }
    }
  },
  "Unauthorized": {
    "description": "Missing or invalid bearer token. Occurs only under `SERVICE_AUTH_MODE=oidc`; with the default `SERVICE_AUTH_MODE=trusted_network` the untokened call is accepted and this response does not occur (RFC-011).",
    "content": {
      "application/json": {
        "schema": { "$ref": "./schemas/common.json#/Error" }
      }
    }
  },
  "Forbidden": {
    "description": "Authenticated but the token lacks the `setpoints:write` service role (or the operator role) required to submit setpoints. Occurs only under `SERVICE_AUTH_MODE=oidc` (RFC-011, platform security §4).",
    "content": {
      "application/json": {
        "schema": { "$ref": "./schemas/common.json#/Error" }
      }
    }
  }
}
