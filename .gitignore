# Research notes (local only)
research/

# Rust
# Unanchored so it also matches climate-controller/target/ (the crate is not at repo root).
target/
**/*.rs.bk
# Cargo.lock is committed: the controller is a binary, so the lockfile pins reproducible builds.

# Node / frontend
node_modules/
dist/
*.log

# Playwright (e2e) artifacts
**/test-results/
**/playwright-report/
**/blob-report/
**/.playwright/

# Lighthouse CI reports (frontend gate)
**/.lighthouseci/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment / secrets
.env
.env.*
!.env.example

# Generated controller fleet (deploy/scripts/gen-controllers.sh)
deploy/docker-compose.override.yml
deploy/controllers/
# Generated Prometheus file-SD targets for the controller fleet (same generator)
deploy/prometheus/targets/

# OS / editor
.DS_Store
Thumbs.db

# Claude
.claude/scratch/
.claude/history/
