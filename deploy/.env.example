# Copy to deploy/.env and set a password before bringing up the stack:
#   cp deploy/.env.example deploy/.env
#
# Used by deploy/docker-compose.yml for the TimescaleDB superuser and the platform's DSN.
POSTGRES_PASSWORD=greenhouse
