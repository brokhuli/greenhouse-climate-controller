# Auto-provisioned Prometheus datasource (operations §1). Grafana reaches Prometheus over the
# internal Docker network; no manual setup on first boot.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
